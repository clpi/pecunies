# TODO

- Add real long-form posts and richer RSS entries.
- Add full chess rules, legal move validation, check/checkmate detection, and scoring.
- Expand minesweeper with recursive empty-square opening and difficulty levels.
- Add richer fake `/internet` navigation with back/forward state and more sites.
- Wire `/book` to a verified outbound email provider or calendar API if Cloudflare email constraints require it.
- Add private authenticated metrics export.
- Add richer markdown rendering for OS files such as `/README.md` and `/TODO.md`.
- Expand `down.nvim` project notes as the plugin evolves.
- Add more command examples to `man` pages.
