{
  "name": "pecunies",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:wasm": "node scripts/build-wasm.mjs",
    "test:sanity": "node scripts/sanity-check.mjs",
    "preview": "vite preview",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name pecunies-chaos-portfolio"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "dompurify": "^3.4.1",
    "marked": "^18.0.2"
  },
  "optionalDependencies": {
    "@rolldown/binding-darwin-universal": "npm:null@*"
  }
}
