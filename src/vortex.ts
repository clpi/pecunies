/**
 * Back-compat re-exports — the ambient field is implemented in `particle-field.ts`.
 */
export {
  mountParticleField,
  mountVortexField,
  type ParticleFieldOptions,
  type ParticlePreset,
} from './particle-field';
